import React from 'react';

const WeatherList = ({weatherList}) => {
  //const icon = weatherList?.weather[0].icon;
  return (
    <>
       {/* <h1 className='wltemp'><img src={`${ICON_URL}/${icon}@2x.png`} alt="thepic"/></h1>
       <p>{`${weatherList?.main?.temp}`}</p> */}
       <button>click</button>
    </>
  );
};

export default WeatherList;
//{}  []